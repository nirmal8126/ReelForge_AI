{
  "buildCommand": "npx prisma generate --schema=packages/db/prisma/schema.prisma && npm run build --workspace=@reelforge/web",
  "outputDirectory": "apps/web/.next",
  "installCommand": "npm install --ignore-scripts",
  "framework": "nextjs"
}
