[build]
builder = "DOCKERFILE"
dockerfilePath = "../../apps/worker/Dockerfile"
watchPatterns = ["apps/worker/**", "packages/db/**"]

[deploy]
startCommand = "node dist/index.js"
healthcheckPath = "/"
healthcheckTimeout = 300
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 5
